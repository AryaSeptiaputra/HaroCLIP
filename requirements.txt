# HaroClip — core dependency skeleton
# NOTE: not yet installed. Heavy packages (torch, ultralytics, faster-whisper)
# need a CUDA build matched to the target GPU (24GB VRAM budget) before install.

# --- API / backend ---
fastapi
uvicorn[standard]
pydantic
python-dotenv
httpx

# --- Video / audio processing ---
ffmpeg-python
opencv-python
numpy

# --- Speech-to-text (heavy, GPU-oriented — confirm before installing) ---
# faster-whisper

# --- Highlight detection LLM ---
# Claude API (Anthropic) — not a local/GPU model. Decided 2026-07-25: cost/complexity
# analysis (context-window math, download-bottleneck risk, GPU budget) favored a
# hosted API over a local model for this stage. See CLAUDE.md.
anthropic

# --- Detection / tracking (heavy, GPU-oriented — confirm before installing) ---
# ultralytics       # YOLOv8-face
# Light-ASD: no PyPI package — vendored under src/detection/light_asd/ (MIT, see NOTICE.md there)
# python_speech_features   # MFCC extraction for Light-ASD (small, pure-Python, but only used by the ASD path)
# supervision        # ByteTrack wrapper (or vendor ByteTrack directly)

# --- ML runtime (install separately with correct CUDA build) ---
# torch
# torchvision

# --- Ingestion / link resolution ---
yt-dlp

# --- Persistence ---
sqlalchemy

# --- Dev / testing ---
pytest
